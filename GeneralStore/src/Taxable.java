public interface Taxable {
    public static final double CA_TAX_RATE = 0.1;
    public double calcTax(); //only use interface when all of ur classes have the same method, kind of like how all cars must have seatbelts, serves as a reminder ig
} //interface = all classes must have it
//basically a list of method headers

